
function SideBar(props) {
  return (
    <nav id="sidebar">
      <ul>
        <button>Board</button>
        <button>Add Task</button>
        <button>About</button>
      </ul>
    </nav>
  )
}

export default SideBar